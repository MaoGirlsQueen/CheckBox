<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haocent.android.checkbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".single.SingleActivity"
            android:label="单选"
            android:screenOrientation="portrait" />

        <activity
            android:name=".multi.MultiActivity"
            android:label="多选"
            android:screenOrientation="portrait" />

        <activity
            android:name=".multi.listorder.MultiListOrderActivity"
            android:label="多选：列表顺序"
            android:screenOrientation="portrait" />

        <activity
            android:name=".multi.selectorder.MultiSelectOrderActivity"
            android:label="多选：选择顺序"
            android:screenOrientation="portrait" />

        <activity
            android:name=".multi.listorder.MultiListOrderChildActivity"
            android:label="多选（列表的顺序）选择的内容"
            android:screenOrientation="portrait" />

        <activity
            android:name=".multi.selectorder.MultiSelectOrderChildActivity"
            android:label="多选（选择的顺序）选择的内容"
            android:screenOrientation="portrait" />

        <activity
            android:name=".selectall.SelectAllActivity"
            android:label="全选"
            android:screenOrientation="portrait" />

        <activity
            android:name=".invert.InvertActivity"
            android:label="反选"
            android:screenOrientation="portrait" />

    </application>

</manifest>